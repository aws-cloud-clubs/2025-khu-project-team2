spring:
  kafka:
    bootstrap-servers: localhost:9092

broker:
  main-url: http://localhost:8080/broker-order
  sub-url: http://localhost:8080/sub-broker-order
  sub:
    max-requests: 10

management:
  metrics:
    export:
      cloudwatch:
        enabled: false